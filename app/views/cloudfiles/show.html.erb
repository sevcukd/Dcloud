Назва файлу: <%= @cloudfile.name %><br />
Опис: <%= @cloudfile.description %><br />
   		<%= link_to "Завантажити", @cloudfile.ffile.url %> |

	<% if @cloudfile.user_id == current_user.id %>
		<%= link_to "Редагувати", edit_cloudfile_path(@cloudfile.id) %> |
		<%= link_to "Видалити", cloudfile_path(@cloudfile.id), method: :delete, data: {confirm: "Ти впевнений?"}  %> |

	<% end %>